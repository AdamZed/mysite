interface Project {
  name: string;
  desc: string;
  stack: string[];
  repo?: string;
  link?: string;
  date?: string;
}

const projectList: Project[] = [];

export default function Projects() {
  return <></>;
}
